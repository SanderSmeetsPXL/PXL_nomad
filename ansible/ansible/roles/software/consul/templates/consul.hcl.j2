bind_addr = "{{ ansible_eth1.ipv4.address }}"
data_dir = "/tmp/consul"
UI=true
{% if "servers" in group_names %}
server = true
bootstrap_expect = 1
{% else %}
retry_join = ["{{ consul_server_ip }}"]
{% endif %}

